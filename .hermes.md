# HermesUsage —— 项目宪章（Hermes 自动注入本文件）

本文件在**任何**子目录下运行 Hermes 时都会自动进入系统提示（`.hermes.md` 会向上
走到 git 根，`AGENTS.md` 只管当前目录）。所以：**改规则就改这里，别写在别处。**

## 0. 这个项目是什么

面向初学者的 Hermes Agent 教程系统。三条不可动摇的定位：

1. **可考证** —— 每句事实性陈述都能反查到官方文档的 URL + 版本 + 内容哈希。
2. **可自维护** —— 规范写在脚本里（`scripts/verify.py`），不写在散文里；AI 跑一条命令就知道自己改得对不对。
3. **可回滚** —— 每次对话/阶段都有 journal 记录 + 独立 git 提交 + 里程碑 tag。

读者画像：第一次听说 Hermes、英语一般、只有 30 分钟。**默认他什么都不知道。**

## 1. 目录地图

```
lessons/<阶段目录>/L<xx>-<slug>.md   课程（唯一的教学内容载体）
sources/registry.yaml                来源登记表（唯一需要人工维护的出处文件）
sources/cache/<id>.md                官方文档快照（自动生成，禁止手改）
sources/citations.yaml               出处元数据 URL/版本/hash（自动生成，禁止手改）
scripts/verify.py                    质量门禁（提交前必跑）
scripts/build_index.py               生成 llms.txt 索引（AI 的导航入口）
scripts/build_map.py                 生成 docs/learning-map.html（初学者的可视化地图）
scripts/sync_sources.py              刷新官方文档快照
scripts/journal.py                   “总结 → 提交”的归档器（含 autocommit 无人值守模式）
scripts/progress.py                  学习者本地进度打卡
templates/lesson.md                  课程模板 = 校验规范的可读版本
templates/journal.md                 归档模板
journal/                             会话/阶段总结（倒序索引在 journal/INDEX.md）
progress/checklist.md                给学习者的打勾清单
docs/learning-map.html               可视化学习地图（可点击，由 build_map.py 生成）
web/                                 站点前端源码（模板 / CSS / 原生 ES 模块；无 npm 依赖）
                                     注意：站点源码在 web/，构建产物在 site/（site/ 不进 git）
tests/                               Python 单元测试（unittest）与前端测试（tests/js，node --test）
site/                                构建产物（由 scripts/build_site.py 生成，已 gitignore）
scripts/build_site.py                生成静态站点 + 站内链接自检
scripts/serve.py                     本地预览（正确 MIME，别用 python -m http.server 顶替）
scripts/check.py                     一条命令跑完全部检查（CI 与提交前用这个）
.hermes/skills/*/SKILL.md            本项目自带技能（需要 `hermes skills trust .` 才加载）
llms.txt                             机器可读总目录（由脚本生成）
```

## 2. 硬规则（违反即 verify 失败）

**R1 课程结构固定 8 节**：`你将学会` / `先动手` / `原理` / `亲手验证` / `常见坑` /
`试一试` / `下一步` / `出处`。顺序固定，标题文字可微调但必须包含这些关键词。
理由：初学者需要稳定的认知脚手架，不能每课换套路。

**R2 先动手后原理**。任何一课的 `先动手` 必须在 3 分钟内给出**可见的结果**。
禁止用「首先安装…然后配置…最后你就理解了」这种铺垫式开篇。

**R3 出处硬绑定**。正文里每个事实性断言后面跟 `[[src:<id>]]`；`<id>` 必须：
- 在 `sources/registry.yaml` 登记过；
- 出现在该课程 frontmatter 的 `sources:` 里（两边必须完全一致）；
- 在文末 `## 出处` 小节列出可点击的官方 URL。
**禁止凭记忆写 Hermes 的行为**。不确定就打开 `sources/cache/` 找原文，或者去读
`$HERMES_HOME/hermes-agent/website/docs/` 的源码。

**R4 命令与预期输出成对出现**。只给命令不给“你应该看到什么”，读者无法自我判断。

**R5 中文正文，代码/命令/参数/文件路径保持英文原样**。不要把 `hermes config set`
翻译成“赫耳墨斯配置设置”。

**R6 不用无法验证的形容词**。删掉「强大」「优雅」「极致」。用动词 + 可观察结果。

**R7 frontmatter 必填**：`id, title, stage, level, minutes, prereq, tags, sources, updated`。
`level` 只有 `入门` / `进阶`；`id` 形如 `L10`；文件名必须以 `<id>-` 开头。

**R8 编码统一 UTF-8 无 BOM、换行 LF**（Windows 上最容易踩，`.gitattributes` 已兜底，
但用 Python 写文件时必须显式 `encoding="utf-8"`、`newline="\n"`）。

## 3. 每次改完必须跑的检查

```bash
python scripts/check.py            # 全量：门禁 + 单元测试 + 站点自检（最省事的入口）
python scripts/build_index.py      # 新增/删除课程后：重建 llms.txt
python scripts/verify.py           # 内容门禁：11 条规则全绿才算改完
python scripts/verify.py --fix-hint  # 门禁没过时看修复建议
python scripts/build_site.py       # 改了 web/ 或课程后：重建站点
```

> **站点与课程的关系**：`lessons/` 是唯一内容源，`site/` 是它的编译产物。
> 改课程后 `python scripts/check.py` 会验证站点能重建、站内链接无死链。
> 不要手改 `site/` 里的任何文件（它在 .gitignore 里，改了也会被下次构建覆盖）。

**没跑过 `python scripts/check.py`（或至少 verify.py）的改动不算改完。** 这是本项目的“测试”。

## 4. git 工作流（每个对话/阶段都要归档）

提交粒度 = 一个可回滚的语义单元。

```bash
# ① 会话收尾（改了课程/脚本）
python scripts/journal.py commit --kind session --title "补 L11 工具系统" \
    --scope L11 --summary "新增一课；修正 frontmatter" --learned "registry.yaml 要手改，citations 会自动生成"

# ② 阶段收尾（一整批课程完成）
python scripts/journal.py commit --kind stage --title "阶段0 认识 Hermes 完成" \
    --scope L00,L01,L02,L03 --with-session-digest
git tag -a v0.1-orient -m "阶段0：认识 Hermes"

# ③ 想看/想回滚
python scripts/journal.py log
python scripts/journal.py rollback --list
```

规则：
- **每个 journal 条目对应一次独立提交**，commit message 前缀用 `session:` / `stage:` /
  `fix:` / `docs:` / `release:`（journal.py 会用 `--kind` 自动加）。
- 阶段完成必须打 tag，tag 名 `v<主>.<次>-<阶段名>`。
- 回滚用 `git revert`（保留历史）或 `git checkout <sha> -- <file>`（单文件），
  **不要用 `--force` 重写历史**。

### 无人值守归档（可选）

`python scripts/journal.py autocommit` 是给 cron / hook 用的静默模式：
工作区无改动时**不输出任何内容**（因此不会产生投递噪音），有改动才写一条记录并提交。

已注册的 cron 任务（`no_agent` 脚本模式，每小时）：

```bash
hermes cron list                          # 看状态（当前是 paused）
hermes cron resume <job_id>               # 启用
hermes cron remove <job_id>               # 不想要就删掉
```

包装脚本在 `$HERMES_HOME/scripts/hu-autojournal.py`。注意：**cron 需要网关在跑**
（`hermes gateway start`），否则任务只是登记着、不会触发。

## 5. AI 接手时的标准动作（按顺序）

1. 读本文件 → 读 `ROADMAP.md` 看当前进度（哪些课已写完、哪些是空的占位）。
2. `python scripts/verify.py` 看当前是否健康；不健康先修，别在坏地基上加东西。
3. 要新增/修改课程：复制 `templates/lesson.md`，填 frontmatter，写正文。
4. 写正文前**先读** `sources/cache/<id>.md` 原文。缓存里没有的主题，先在
   `sources/registry.yaml` 登记并跑 `python scripts/sync_sources.py`。
5. 改完跑第 3 节的检查，全绿后 `python scripts/journal.py commit ...`。
6. 阶段完成：`git tag`。

## 6. 已知坑（踩过的，别重踩）

| 现象 | 真实原因 | 处理 |
|---|---|---|
| 门禁报“快照哈希不一致” | 手改了 `sources/cache/` 里的文件 | 别改，重跑 `sync_sources.py` |
| 本机门禁全绿，CI（Linux）却报 R10 快照哈希不一致 | 快照在工作区里是 CRLF、git 里存的是 LF，哈希是按 CRLF 算的 | 已修：`sync_sources.py` 一律按 LF 落盘并算哈希；R10 现在会直接报出 CRLF |
| 文档路径失效 | registry 里写的路径不对 | `sync_sources.py --list-missing` 定位 |
| 中文乱码 / 尾部多个 `^M` | 写文件时用了默认编码或 CRLF | Python 写文件带 `encoding` + `newline="\n"` |
| 项目技能不生效 | 项目级技能默认不加载（防投毒） | `hermes skills trust .` 后新开会话 |
| `AGENTS.md` 里的规则在子目录不生效 | `AGENTS.md` 只看 cwd | 规则写 `.hermes.md`（会向上找到 git 根） |
| 引用官方文档说法但记不清 | 凭记忆写 | 打开 `sources/cache/` 或 `$HERMES_HOME/hermes-agent/website/docs/` |
| 索引生成器崩在 `found character '@'` | YAML 流式序列里 `@` 开头的值非法 | `tags:` 一律写带引号的形式：`tags: ["@引用", "AGENTS.md"]` |
| 项目技能不出现在 `hermes skills list`，也不加载 | 项目技能按「项目根」解析，而项目根优先采用会话工作目录 `TERMINAL_CWD`；落在家目录就解析不到项目 | `hermes skills trust .` 之后**从项目目录启动会话**；自查脚本见 `lessons/01-core/L15-skills.md` 的「亲手验证」 |
| 读 `$HERMES_HOME/hermes-agent/` 下的文件把上下文塞爆 | 安装树里的 `AGENTS.md` 会被当子目录提示注入 | 别直接读安装树；读 `sources/cache/<id>.md` 快照 |
| 站点页面交互全哑（搜索、打勾没反应） | 用了 `python -m http.server`，`.js` 的 MIME 不对，ES 模块被浏览器拒绝 | 改用 `python scripts/serve.py` |
| 站点里出现 `{{footer}}` 这样的字面量 | 模板占位符没填 | build_site.py 会直接报错并指出占位符名，补 `render_template` 的 values |

## 7. 环境事实（本机）

- Hermes 源码：`$HERMES_HOME/hermes-agent`（`HERMES_HOME` 默认 `%LOCALAPPDATA%\hermes`）
- 官方文档源码：`$HERMES_HOME/hermes-agent/website/docs/`（89 条登记来源的快照来源）
- 出处快照基线：`hermes v0.21.3`，文档提交 `05fac10a`（2026-09-15）
- 运行环境：Windows 11 + Git Bash；Python 3.11（有 PyYAML）；node 24
- node 的用法：前端单测只能写成 `node --test "tests/js/*.test.js"` —— Node ≥ 22 起
  `--test` 的位置参数按 glob 解释，**不展开目录**，写 `node --test tests/js` 会报
  `Cannot find module`。站点构建本身**不需要 npm 依赖**（`web/assets/lib/` 里是随仓库走的原生 ES 模块），
  `package.json` 无 dependencies，只提供 `npm test` / `npm run build` / `npm run serve` 三个别名。
- **本机没有 docker**：`docker --version` 报 command not found，所以 `Dockerfile` /
  `docker-compose.yml` 在本机**无法验证** —— 文档和提交信息里不要写「已验证」。
- **远端已存在**：`origin` = https://github.com/mt-yu/HermesUsage（GitHub 账号 `mt-yu`）。
  公开站点：<https://mt-yu.github.io/HermesUsage/>，由 `.github/workflows/pages.yml` 在 push 到 `main` 时自动重建。
  首次发布（仓库创建 + 开 Pages）是通过 API 完成的，凭据取自 Git Credential Manager
  （`git credential fill`，作用域 `repo` + `workflow`，密钥本身不要打印到任何输出里）。
- Python 依赖写在 `requirements.txt`（PyYAML + Markdown，都是纯 Python）；本机的 `python`
  指向 Hermes 自带 venv，这两个包已装好，`pip install -r requirements.txt` 只是确认。
- 官方文档站：https://hermes-agent.nousresearch.com/docs ；全量索引 `/docs/llms.txt`

## 8. 绝对不要做的事

- 不要把 `sources/cache/` 的内容当成自己的原创内容改写后混进课程。
- 不要在课程里编造命令、参数、配置键 —— 没在官方文档或 `hermes --help` 里见过就不要写。
- 不要为了“看起来完整”而占位：没写完的课就不要建文件，宁缺毋滥（ROADMAP 里标 `⬜` 待建）。
- 不要跳过 `verify.py` 直接提交。
