---
date: 2026-09-16
kind: stage
scope: L10,L11,L12,L13,L14,L15
title: 阶段1 核心五件事完成 6 课
commit: df67eae
---

# 2026-09-16 · 阶段1 核心五件事完成 6 课

## 目标

这次会话开始时我想做什么（一句话）。

## 实际做了什么

- 新增 lessons/01-core/ 六课：L10 模型与Provider、L11 工具与工具集、L12 会话与斜杠命令、L13 记忆、L14 上下文文件、L15 技能
- 每课都有本机实测验证：hermes tools list 真实输出、-t web 限制下模型诚实承认没有终端工具、state.db 里的真实消息序列
- L15 记录了一个实测坑：项目技能除了 hermes skills trust . 之外，还要求会话工作目录（TERMINAL_CWD）落在项目内，否则项目根解析为 None
- 门禁新增两处拦截并修复：未声明的正文引用（R3）、YAML 流式序列里 @ 开头值的语法错误

## 学到的东西

> 这一节是 journal 存在的理由。**留给未来的自己**：
> 什么踩坑了、什么反直觉、哪条命令救了我。

- state.db 是唯一权威会话存储（Windows 上 sessions/ 目录为空）
- 项目技能解析优先级：TERMINAL_CWD > 进程 cwd；home 目录被视为「非项目」按设计跳过
- 项目技能还有第二道关：skills_guard 扫描，dangerous 判定会隔离，扫描器崩了 fail-closed 也隔离；缓存在 HERMES_HOME/cache/project_skill_scans
- 不要用 read_file 读 Hermes 安装树里的文件：会触发子目录 AGENTS.md 注入，把上下文塞满
- YAML 流式序列里以 @ 开头的标量非法，tags 要带引号

## 引用到的官方出处

- [[src:quickstart]] — 我在这页确认了 XXX

## 下一步

- [ ] 

## 回滚指引

```bash
git log --oneline                      # 找到要回退到的提交
git revert <sha>                       # 安全回滚（保留历史）
git checkout <sha> -- <file>           # 只回滚某个文件
```
