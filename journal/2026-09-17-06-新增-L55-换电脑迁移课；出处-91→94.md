---
date: 2026-09-17
kind: session
scope: L55
title: 新增 L55 换电脑迁移课；出处 91→94
commit: a6538d7
---

# 2026-09-17 · 新增 L55 换电脑迁移课；出处 91→94

## 目标

这次会话开始时我想做什么（一句话）。

## 实际做了什么

新增 L55「换一台电脑：把记忆、技能和会话带走」（阶段 5，25 分钟，8 小节，全部命令配本机真实输出）；登记 profile-commands / profile-distributions / multi-connection-desktop 三页出处（91→94）并重建快照；课程 38→39 课、坑表 312→324 行、离线版 833 KB 等派生数字全部同步；补 .hermes.md 的 HERMES_HOME 路径坑。注：本次改动先被 16:00 的自动归档卷进 f91599a（消息不含语义），本条记录与 a6538d7 指向同一批改动

## 学到的东西

> 这一节是 journal 存在的理由。**留给未来的自己**：
> 什么踩坑了、什么反直觉、哪条命令救了我。

profile export 的 default 走允许清单、不含 state.db（会话历史只有整机 backup 带得走）；hermes import 在非默认 home 会显式保留新机运行时文件；Git Bash 里 HERMES_HOME 用 /c/... 风格会被解释成盘符相对路径并静默指向空 home

## 引用到的官方出处

- [[src:quickstart]] — 我在这页确认了 XXX

## 下一步

- [ ] 

## 回滚指引

```bash
git log --oneline                      # 找到要回退到的提交
git revert <sha>                       # 安全回滚（保留历史）
git checkout <sha> -- <file>           # 只回滚某个文件
```
