---
date: 2026-09-17
kind: session
scope: L56
title: 新增 L56 公司机与个人机：一个内核，两个身份
commit: 8747423
---

# 2026-09-17 · 新增 L56 公司机与个人机：一个内核，两个身份

## 目标

这次会话开始时我想做什么（一句话）。

## 实际做了什么

阶段 5 新增第 7 课 L56：用 profile 做公司/个人身份隔离（含官方逐项隔离表摘要）、三种布局对比（只用 work / work + personal / 公司机当客户端）、能带回家与一律不带的归属清单、公司机收紧清单（approvals manual + deny + HERMES_WRITE_SAFE_ROOT + docker/ssh 后端 + 网关 allowlist）、以及撤离流程（export 剥离凭证 → delete → 清残留）。本机隔离 HOME 实测：--clone 会把个人 .env 与 MEMORY.md 拷进新 profile（陷阱，已写成先动手实验）、profile show/list 目录结构、删除后留 .deleted 墓碑。出处 94→95（新增 multi-profile-gateways），课程 39→40 课

## 学到的东西

> 这一节是 journal 存在的理由。**留给未来的自己**：
> 什么踩坑了、什么反直觉、哪条命令救了我。

公司/个人身份的正确隔离单位是 profile 而不是机器；--clone 会连 .env 与 memories 一起复制，公司 profile 应当空建；官方明说 deny 规则是护栏不是沙箱，要真隔离就换 docker/ssh 后端

## 引用到的官方出处

- [[src:quickstart]] — 我在这页确认了 XXX

## 下一步

- [ ] 

## 回滚指引

```bash
git log --oneline                      # 找到要回退到的提交
git revert <sha>                       # 安全回滚（保留历史）
git checkout <sha> -- <file>           # 只回滚某个文件
```
