---
date: 2026-09-17
kind: stage
scope: L60,L61,L62,L63,L64,L65
title: 阶段 6 真实工作流案例（6 课）+ 漂移哨兵进 CI + 自动归档闸门
commit: beea79d
---

# 2026-09-17 · 阶段 6 真实工作流案例（6 课）+ 漂移哨兵进 CI + 自动归档闸门

## 目标

这次会话开始时我想做什么（一句话）。

## 实际做了什么

六课：L60 只读侦察清单化 / L61 不可逆批量操作的安全做法 / L62 只在变化时通知 / L63 带出处调研报告 / L64 固化成技能 / L65 并行委派与验收；配套：CI 漂移哨兵每天 09:00（稀疏克隆上游 docs 比对快照哈希，首次运行即开 issue #1 报 37 页已变）、journal autocommit 加在途改动闸门、STAGES/build_index/ROADMAP/checklist/三处计数金丝雀同步

## 学到的东西

> 这一节是 journal 存在的理由。**留给未来的自己**：
> 什么踩坑了、什么反直觉、哪条命令救了我。

hermes chat -q 的工作目录要 --in 指定；cron list 默认隐藏 paused；自动归档会把在途改动卷成提交（已用代码强制拦截）；mock 掉 run 的单元测试看不见 cwd 冲突这类调用层 bug，集成测试必须不打 mock

## 引用到的官方出处

- [[src:quickstart]] — 我在这页确认了 XXX

## 下一步

- [ ] 

## 回滚指引

```bash
git log --oneline                      # 找到要回退到的提交
git revert <sha>                       # 安全回滚（保留历史）
git checkout <sha> -- <file>           # 只回滚某个文件
```
