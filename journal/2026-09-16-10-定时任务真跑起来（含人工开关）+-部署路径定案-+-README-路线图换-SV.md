---
date: 2026-09-16
kind: session
scope: scripts/cron_ctl.py,.github/workflows/docker.yml,scripts/build_roadmap_svg.py
title: 定时任务真跑起来（含人工开关）+ 部署路径定案 + README 路线图换 SVG
commit: 226b322
---

# 2026-09-16 · 定时任务真跑起来（含人工开关）+ 部署路径定案 + README 路线图换 SVG

## 目标

这次会话开始时我想做什么（一句话）。

## 实际做了什么

网关装成登录自启，cron 真正触发；新增 cron_ctl.py 总开关与漂移/外链两个任务；删除 Netlify/Vercel 配置并把 Docker 交给 CI 真构建验证；README 路线图换成由课程生成的 SVG（check.py 升到 6 项）

## 学到的东西

> 这一节是 journal 存在的理由。**留给未来的自己**：
> 什么踩坑了、什么反直觉、哪条命令救了我。

hermes cron --script 只收 $HERMES_HOME/scripts 下的相对文件名；cron list 默认不显示已暂停任务（要 --all）；自动归档会把子代理在途改动卷成自动提交；len(str) 不是字节数

## 引用到的官方出处

- [[src:quickstart]] — 我在这页确认了 XXX

## 下一步

- [ ] 

## 回滚指引

```bash
git log --oneline                      # 找到要回退到的提交
git revert <sha>                       # 安全回滚（保留历史）
git checkout <sha> -- <file>           # 只回滚某个文件
```
