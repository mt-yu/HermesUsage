---
date: 2026-09-21
kind: session
scope: L12,L14,L31,L50,L51,ROADMAP
title: 审计「课程有没有超前」：5 处改成 release 行为（L12/L14/L31/L50×2/L51）
commit: 135b4e5
---

# 2026-09-21 · 审计「课程有没有超前」：5 处改成 release 行为（L12/L14/L31/L50×2/L51）

## 目标

这次会话开始时我想做什么（一句话）。

## 实际做了什么

43 页被课程引用的「快照独有」内容逐页判定后，按 v2026.9.14 源码复核：webhook coalesce / 项目 .env 语义反转 / 挖历史打码 / 编号环境变量 / /context 逐文件清单 —— 这 5 处在 release 里确实没有，已从课程删掉或改成 release 行为；另 5 处（kanban non-spawnable、引号解析 fail-closed、use_real_profile、/learn、契约不达标仍 completed）经源码核实 release 里就有，不改

## 学到的东西

> 这一节是 journal 存在的理由。**留给未来的自己**：
> 什么踩坑了、什么反直觉、哪条命令救了我。

文档缺一句话不等于功能不存在，反过来也一样 —— 「上游文档写了」必须按 tag 的源码复核，别只看文档；判据拆成「该不该改课程看 release」与「该不该复核看有没有新 release」之后，两次审计（过时/超前）才是完整的一对

## 引用到的官方出处

- [[src:quickstart]] — 我在这页确认了 XXX

## 下一步

- [ ] 

## 回滚指引

```bash
git log --oneline                      # 找到要回退到的提交
git revert <sha>                       # 安全回滚（保留历史）
git checkout <sha> -- <file>           # 只回滚某个文件
```
