---
date: 2026-09-16
kind: release
scope: web,site
title: 教程站：可交互前端 + 五种部署方式
commit: 3377a22
---

# 2026-09-16 · 教程站：可交互前端 + 五种部署方式

## 目标

这次会话开始时我想做什么（一句话）。

## 实际做了什么

新增静态站点（按阶段浏览 / 全文搜索 / 进度打卡 / 出处徽标可核对）+ Docker/Netlify/Vercel/Pages 部署 + 一条命令全量检查 check.py；抽出 tutorial_core 作为唯一解析层

## 学到的东西

> 这一节是 journal 存在的理由。**留给未来的自己**：
> 什么踩坑了、什么反直觉、哪条命令救了我。

中文标题不能依赖 markdown 的 toc 扩展（slugify 会吞掉中文）；data-lesson 这种属性选择器会把 body 的空值一起选中；CSS 的 display 会盖掉 hidden 属性

## 引用到的官方出处

- [[src:quickstart]] — 我在这页确认了 XXX

## 下一步

- [ ] 

## 回滚指引

```bash
git log --oneline                      # 找到要回退到的提交
git revert <sha>                       # 安全回滚（保留历史）
git checkout <sha> -- <file>           # 只回滚某个文件
```
