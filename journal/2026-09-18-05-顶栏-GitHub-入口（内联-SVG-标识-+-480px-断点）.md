---
date: 2026-09-18
kind: session
scope: site,scripts,web
title: 顶栏 GitHub 入口（内联 SVG 标识 + 480px 断点）
commit: 0962a02
---

# 2026-09-18 · 顶栏 GitHub 入口（内联 SVG 标识 + 480px 断点）

## 目标

这次会话开始时我想做什么（一句话）。

## 实际做了什么

顶栏最右加一枚 GitHub 标识（内联 SVG、新标签页打开、repo_url 空则不渲染），页脚复用同一枚；480px 以下收掉进度胶囊；顺带修掉 render_template 把正文里的 {{…}} 当成模板没填的坑

## 学到的东西

> 这一节是 journal 存在的理由。**留给未来的自己**：
> 什么踩坑了、什么反直觉、哪条命令救了我。

文档里讨论模板占位符（{{github}}/{{footer}}）会让旧版 render_template 判定为未填、整站构建失败：判定只该看模板骨架

## 引用到的官方出处

- [[src:quickstart]] — 我在这页确认了 XXX

## 下一步

- [ ] 

## 回滚指引

```bash
git log --oneline                      # 找到要回退到的提交
git revert <sha>                       # 安全回滚（保留历史）
git checkout <sha> -- <file>           # 只回滚某个文件
```
