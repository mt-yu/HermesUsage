---
date: 2026-09-17
kind: session
scope: L55
title: L55 补最后一击实测：换机后的 agent 认得你
commit: 6555979
---

# 2026-09-17 · L55 补最后一击实测：换机后的 agent 认得你

## 目标

这次会话开始时我想做什么（一句话）。

## 实际做了什么

L55 亲手验证新增【最后一击】：在从 zip 恢复的全新临时 home 里真跑两次对话 —— 一次凭长期记忆回答用户偏好（0 tool calls，证明记忆被用上而非只是文件在），一次用 session_search 翻出迁移前的两段旧会话（26 messages / 24 tool calls，证明状态库连同全文索引一起过来了）；并显式标注诚实边界（同一台机器换 HERMES_HOME 的实测，不代表跨平台开箱即用）。check.py 六项全绿

## 学到的东西

> 这一节是 journal 存在的理由。**留给未来的自己**：
> 什么踩坑了、什么反直觉、哪条命令救了我。

文件在 ≠ 习惯在：判定迁移是否成功要跑一次真对话（0 tool calls 答出偏好）与一次 session_search（翻出迁移前的会话），只比哈希只能证明数据搬到了

## 引用到的官方出处

- [[src:quickstart]] — 我在这页确认了 XXX

## 下一步

- [ ] 

## 回滚指引

```bash
git log --oneline                      # 找到要回退到的提交
git revert <sha>                       # 安全回滚（保留历史）
git checkout <sha> -- <file>           # 只回滚某个文件
```
