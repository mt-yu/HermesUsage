---
date: 2026-09-16
kind: stage
scope: L00,L01,L02,L03
title: 阶段0 认识 Hermes 完成 4 课
commit: 3cb918b
---

# 2026-09-16 · 阶段0 认识 Hermes 完成 4 课

## 目标

这次会话开始时我想做什么（一句话）。

## 实际做了什么

- 新增 lessons/00-orient/ 四课：L00 三个心智模型、L01 第一次对话、L02 agent loop 拆解、L03 文件地图
- 门禁首次全绿：build_index + verify（修了 3 个真 bug：YAML 日期解析成 date、前向引用误报、相对链接层级）
- 所有预期输出都是本机真实跑出来的，包括 state.db 里的真实消息序列

## 学到的东西

> 这一节是 journal 存在的理由。**留给未来的自己**：
> 什么踩坑了、什么反直觉、哪条命令救了我。

- state.db 是唯一权威会话存储；Windows 上 sessions/ 目录是空的，不是 jsonl
- PyYAML 把裸写的 2026-09-16 解析成 datetime.date，日期校验必须兼容两类
- 分阶段写作必然产生前向引用，门禁要认 ROADMAP 里已声明的课程 id 而不是只认文件存在
- journal 记录里回填 SHA 不能用 --amend（SHA 会再变），要拆成「工作提交 + 记录提交」两段

## 引用到的官方出处

- [[src:quickstart]] — 我在这页确认了 XXX

## 下一步

- [ ] 

## 回滚指引

```bash
git log --oneline                      # 找到要回退到的提交
git revert <sha>                       # 安全回滚（保留历史）
git checkout <sha> -- <file>           # 只回滚某个文件
```
