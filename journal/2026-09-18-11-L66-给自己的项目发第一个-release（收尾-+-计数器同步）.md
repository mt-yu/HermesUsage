---
date: 2026-09-18
kind: session
scope: L66
title: L66 给自己的项目发第一个 release（收尾 + 计数器同步）
commit: e8e4dd6
---

# 2026-09-18 · L66 给自己的项目发第一个 release（收尾 + 计数器同步）

## 目标

这次会话开始时我想做什么（一句话）。

## 实际做了什么

新增 L66 课程并落盘配套产物（ROADMAP/llms.txt/docs/roadmap.svg）；把 tests 里 7 条硬编码计数改成当前值（课数 40→41、常见坑行数 334→340，含 test_repo_total_is_334_rows 改名为 test_repo_total_rows）；python scripts/check.py 7 项全绿

## 学到的东西

> 这一节是 journal 存在的理由。**留给未来的自己**：
> 什么踩坑了、什么反直觉、哪条命令救了我。

新增/删除一课会打破 tests/test_tutorial_core.py 与 tests/test_build_site.py 里的课程数与常见坑行数金丝雀，必须同批改；子代理的异步结果投递要求父会话 runtime 存活，runtime 消失时 state.db 的 async_delegations 行会永久停在 running（子代理其实已完工）

## 引用到的官方出处

- [[src:quickstart]] — 我在这页确认了 XXX

## 下一步

- [ ] 

## 回滚指引

```bash
git log --oneline                      # 找到要回退到的提交
git revert <sha>                       # 安全回滚（保留历史）
git checkout <sha> -- <file>           # 只回滚某个文件
```
