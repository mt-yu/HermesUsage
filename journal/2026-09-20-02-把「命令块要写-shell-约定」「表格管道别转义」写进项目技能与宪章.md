---
date: 2026-09-20
kind: docs
scope: L00,L01,L12
title: 把「命令块要写 shell 约定」「表格管道别转义」写进项目技能与宪章
commit: bdcc6d5
---

# 2026-09-20 · 把「命令块要写 shell 约定」「表格管道别转义」写进项目技能与宪章

## 目标

这次会话开始时我想做什么（一句话）。

## 实际做了什么

hermes-tutorial-authoring 技能升到 1.2.0，坑表新增两条：命令块不写 shell 约定（Windows 新手在 PowerShell 里跑 bash 管道）、表格单元格用反斜杠转义管道（站点会多显示一个反斜杠，读者照抄跑不通）；.hermes.md 坑表同步两行。

## 学到的东西

> 这一节是 journal 存在的理由。**留给未来的自己**：
> 什么踩坑了、什么反直觉、哪条命令救了我。

教训要写在「写下一课时会被加载的地方」：课程宪章（.hermes.md，每次会话都进系统提示）+ 项目技能（写课时加载），只写在 journal 里等于没写。

## 引用到的官方出处

- [[src:quickstart]] — 我在这页确认了 XXX

## 下一步

- [ ] 

## 回滚指引

```bash
git log --oneline                      # 找到要回退到的提交
git revert <sha>                       # 安全回滚（保留历史）
git checkout <sha> -- <file>           # 只回滚某个文件
```
