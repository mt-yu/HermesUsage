---
date: 2026-09-18
kind: session
scope: design.html,app.css,app.js
title: 站点视觉系统重写 + 新增设计对比页（10 方案 × 10 维度等权平均）
commit: 4457bff
---

# 2026-09-18 · 站点视觉系统重写 + 新增设计对比页（10 方案 × 10 维度等权平均）

## 目标

这次会话开始时我想做什么（一句话）。

## 实际做了什么

新增 scripts/design_matrix.py（10 套 UI 方案 × 10 个维度的评分矩阵，唯一数据源）；新增站点页 /design.html（矩阵 + 构建期 SVG 条图 + 令牌取值链 + WCAG 对比度实测 + 出处）与 data/design.json；按赢家 GitHub Primer（4.40）的两条硬规则重写 app.css（三层令牌、不写裸值、无障碍硬约束）；主题改三态并消除暗色首帧闪白；测试 260→316（Python）/ 72→75（前端）；构建 43→52 页

## 学到的东西

> 这一节是 journal 存在的理由。**留给未来的自己**：
> 什么踩坑了、什么反直觉、哪条命令救了我。

app.js 里新加的顶层 const 必须放在 boot(); 之前：写在后面会撞 TDZ 抛 ReferenceError，被吞成未处理的 Promise 拒绝，页面渲染正常但搜索/打勾/进度全部没接线（已加 tests/js/app-module-order.test.js 守卫）；设计对比页的数字必须现算（平均分来自模块、token 值现读 app.css、对比度现算），改样式表不重算会构建失败

## 引用到的官方出处

- [[src:quickstart]] — 我在这页确认了 XXX

## 下一步

- [ ] 

## 回滚指引

```bash
git log --oneline                      # 找到要回退到的提交
git revert <sha>                       # 安全回滚（保留历史）
git checkout <sha> -- <file>           # 只回滚某个文件
```
