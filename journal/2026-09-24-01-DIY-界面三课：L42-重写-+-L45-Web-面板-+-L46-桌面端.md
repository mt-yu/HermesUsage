---
date: 2026-09-24
kind: session
scope: L42,L45,L46
title: DIY 界面三课：L42 重写 + L45 Web 面板 + L46 桌面端
commit: ed5d321
---

# 2026-09-24 · DIY 界面三课：L42 重写 + L45 Web 面板 + L46 桌面端

## 目标

这次会话开始时我想做什么（一句话）。

## 实际做了什么

L42 改成真正讲终端外观（补 extending-the-cli 的五个钩子）；新增 L45（dashboard 主题 YAML + 插件 + shell slot + plugin_api.py + 令牌门）与 L46（desktop plugin.js + 九类注册区 + CDP 硬门）；出处 95→97；六条文档与实测差异写进常见坑（rescan 401、坏 YAML 静默、plugins.enabled 门、prompt_toolkit filter、HERMES_WEB_DIST、web_dist 路径）；check.py 7 项全绿（Python 445 条），站点 43 课/55 页/73 文件/4271 KB；L46 的 CDP 端口本机未绑上，DOM 级验证未完成并已在正文标注

## 学到的东西

> 这一节是 journal 存在的理由。**留给未来的自己**：
> 什么踩坑了、什么反直觉、哪条命令救了我。

写面板插件时最容易白折腾的不是代码而是三道门：/api/* 要 X-Hermes-Session-Token、user 插件要 plugins.enabled、后端路由要重启进程

## 引用到的官方出处

- [[src:quickstart]] — 我在这页确认了 XXX

## 下一步

- [ ] 

## 回滚指引

```bash
git log --oneline                      # 找到要回退到的提交
git revert <sha>                       # 安全回滚（保留历史）
git checkout <sha> -- <file>           # 只回滚某个文件
```
