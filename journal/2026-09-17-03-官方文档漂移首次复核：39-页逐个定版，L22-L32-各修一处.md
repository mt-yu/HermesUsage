---
date: 2026-09-17
kind: session
scope: L22,L32
title: 官方文档漂移首次复核：39 页逐个定版，L22 / L32 各修一处
commit: 8bcbb63
---

# 2026-09-17 · 官方文档漂移首次复核：39 页逐个定版，L22 / L32 各修一处

## 目标

这次会话开始时我想做什么（一句话）。

## 实际做了什么

依据 issue #1（复核时上游已到 36842e63，39 页 / 486 增 94 删）。判据=该行为在本机装的 v0.21.3 源码里有没有：delegation → L22 改掉「默认没有墙钟超时」，补 450s/1200s 失速线（tools/delegate_tool.py:77-78）；bot-mode → L32 补 message_agent 的双条件门控与 headless 手工开启办法（tools/bot_mode_probe.py，实测空 ui_meta 块即可）。15 页判为上游未发布只登记、22 页无课程影响；ROADMAP 新增「官方文档漂移复核」小节，并补上缺失的 v2.1-ops / v3.0-cases 两行里程碑；issue #1 已留分类结论并关闭。

## 学到的东西

> 这一节是 journal 存在的理由。**留给未来的自己**：
> 什么踩坑了、什么反直觉、哪条命令救了我。

漂移哨兵有两套、比的对象不同：本机那份比「本机装的 Hermes 源码 vs 快照」（所以本机不升级就永远无漂移），CI 那份比「上游 docs vs 快照」——上游 main 永远比最新 release 快，所以复核第一步是 hermes --version 确认本机是不是最新 release，再用本机源码 grep/实测给每一句定版。课程里「没有 X/不会 Y」这类否定断言最容易过时（L22 那句就是），写时宁可写「有 X，阈值是 N」。

## 引用到的官方出处

- [[src:quickstart]] — 我在这页确认了 XXX

## 下一步

- [ ] 

## 回滚指引

```bash
git log --oneline                      # 找到要回退到的提交
git revert <sha>                       # 安全回滚（保留历史）
git checkout <sha> -- <file>           # 只回滚某个文件
```
